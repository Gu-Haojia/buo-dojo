/** Site settings. Set voteUrl to the official voting page when available. */
export const CONFIG = Object.freeze({
  voteUrl: "",
  shareHashtags: ["ぶおー法螺貝道場", "依田芳乃"],
  maxBlowSeconds: 30,
  kanjiIntervalMs: 1000,
});
